import { createTheme } from "@mui/material";

const theme = createTheme({
   palette: {
      primary: {
         main: "#007bff",
      },
      secondary: {
         main: "#15c4d7",
      },
   },
   typography: {
      fontFamily: "Inter",
   },
});

export default theme;
